let firstname = "Pack"; // This is a string literal assigned to the variable 'firstname'
let isStudent = true;
let Full_name = `hi ${firstname} Saini`; // This is a template literal that allows embedding expressions
console.log(Full_name); // Output: hi Pack (with interpolation)